export const metadata = {
    title: 'Track Your Order | Applex Electronics',
    description: 'Track the real-time status of your order at Applex Electronics using your invoice ID.',
};

export default function TrackOrderLayout({ children }) {
    return <>{children}</>;
}
